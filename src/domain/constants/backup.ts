// I version exported backups so future releases can detect older formats
// and migrate or reject them safely instead of importing corrupted data.
export const BACKUP_VERSION = 1
export const BACKUP_APP_ID = 'unrealDice'
